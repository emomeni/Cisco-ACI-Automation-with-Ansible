# Private intent

Copy an example to `config/production.yml`, replace the sample topology, set
`target.environment: production`, the actual APIC DNS name and exact permitted APIC versions.
Align `inventory/production/hosts.yml`. Files here other than this README are ignored by Git.
Keep real configuration in a private repository or a controlled inventory source.
